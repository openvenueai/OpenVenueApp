/**
 * AI daily briefing for the dashboard.
 * Placeholder until AI provider is wired; returns static copy so the UI is ready.
 */
export async function getDailyBriefing(_accountId: string): Promise<string> {
  return "Today’s priorities: review leads that need a response, check overdue tasks, and follow up on any pending proposals. Use the priority rail on the right to jump into action."
}
